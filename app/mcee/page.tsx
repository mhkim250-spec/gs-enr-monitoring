"use client";
import {useCallback,useEffect,useState} from "react";import Sidebar from "../sidebar";
type Item={id:string;title:string;date:string;department:string;url:string};type Group={key:string;name:string;url:string;items:Item[]};
export default function MceePage(){const[groups,setGroups]=useState<Group[]>([]);const[loading,setLoading]=useState(true);const[refreshing,setRefreshing]=useState(false);const[error,setError]=useState("");const load=useCallback(async(refresh=false)=>{if(refresh)setRefreshing(true);setError("");try{const response=await fetch(refresh?`/api/mcee?refresh=${Date.now()}`:"/api/mcee",{cache:"no-store"});const data=await response.json();if(!response.ok)throw new Error(data.error||"기후부 게시물을 불러오지 못했습니다.");setGroups(data.groups||[]);}catch(reason){setError(reason instanceof Error?reason.message:"기후부 연결 실패");}finally{setLoading(false);setRefreshing(false);}},[]);useEffect(()=>{void load();},[load]);return <main className="mcee-page site-content"><Sidebar active="mcee"/><header className="topbar"><div className="topbar-title"><b>기후에너지환경부 모니터링</b></div><button className="refresh-button" onClick={()=>void load(true)} disabled={refreshing}>{refreshing?"업데이트 중":"지금 업데이트"}</button></header><section className="mcee-content"><header className="mcee-hero"><img src="/mcee-logo.png" alt="기후에너지환경부"/><div><span>MINISTRY MONITORING</span><h1>기후·에너지·환경<br/>정책 소식</h1><p>기후에너지환경부의 최신 보도자료와 공지·공고를 확인하세요.</p></div></header>{loading&&<div className="mcee-state"><span className="loader"/>최신 게시물을 불러오고 있습니다.</div>}{error&&<div className="mcee-state error">{error}</div>}<div className="mcee-grid">{groups.map(group=><section className={`mcee-board ${group.key}`} key={group.key}><header><div><span>{group.key==="press"?"PRESS RELEASE":"NOTICE"}</span><h2>{group.name}</h2></div><a href={group.url} target="_blank" rel="noreferrer">전체보기 ↗</a></header><ol>{group.items.map((item,index)=><li key={item.id}><a href={item.url} target="_blank" rel="noreferrer"><b>{String(index+1).padStart(2,"0")}</b><div><strong>{item.title}</strong><span className="mcee-meta"><b>작성자</b><em>{item.department||"기후에너지환경부"}</em><b>등록일</b><em>{item.date}</em></span></div><em>↗</em></a></li>)}</ol></section>)}</div></section></main>}
